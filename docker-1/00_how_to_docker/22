docker-machine ssh Aiur "docker swarm join --token $(docker swarm join-token worker -q) $(docker-machine ip Char):2377"

# Connecting through SSH to Aiur and running command on Aiur (docker-machine ssh Aiur "...")
# Joining to swarm as a worker